# Mail

**Leer cuando:** configuración SMTP multi-tenant, templates HTML, envío vía API key, o logs de envío.

_Todavía no documentado en detalle — leer `MailController.ts` y los casos de uso `CreateMailConfig`/`UpdateMailConfig`/`DeleteMailConfig`/`ListMailConfigs`, `CreateMailTemplate`/`UpdateMailTemplate`/`DeleteMailTemplate`/`ListMailTemplates`, `SendMail`, `ListMailLogs` como fuente de verdad hasta completar este documento._

**También leer:** [api-keys.md](api-keys.md) — el envío vía API key sigue el mismo mecanismo de autenticación que MercadoPago/ARCA/WhatsApp.
