# WhatsApp Business

**Leer cuando:** configuración por número (Meta Cloud API), envío de mensajes, webhook entrante, o historial de mensajes/logs.

_Todavía no documentado en detalle — leer `WhatsAppController.ts` y los casos de uso `CreateWhatsAppConfig`/`UpdateWhatsAppConfig`/`DeleteWhatsAppConfig`/`ListWhatsAppConfigs`, `SendWhatsAppMessage`, `HandleWhatsAppWebhook`, `ListWhatsAppMessages`, `ListWhatsAppLogs`, `AssignApiKeyToWhatsAppConfig`/`UnassignApiKeyFromWhatsAppConfig` como fuente de verdad hasta completar este documento._
