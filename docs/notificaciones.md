# Notificaciones in-app

**Leer cuando:** notificaciones dentro del panel (contador de no leídas, marcar leída/todas, limpiar).

_Todavía no documentado en detalle — leer `NotificationController.ts` y los casos de uso `CreateNotification`, `GetNotifications`, `GetUnreadCount`, `MarkNotificationRead`, `MarkAllNotificationsRead`, `ClearAllNotifications` como fuente de verdad hasta completar este documento. No confundir con WhatsApp ([whatsapp.md](whatsapp.md)) — son canales de entrega distintos, sin código compartido._
