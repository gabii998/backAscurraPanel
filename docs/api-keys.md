# API Keys

**Leer cuando:** emisión o revocación de API keys que autentican llamadas externas/de servicio (mail, MercadoPago, ARCA, WhatsApp, ingesta de errores).

_Todavía no documentado en detalle — leer `ApiKeyController.ts` y los casos de uso `CreateApiKey`, `ListApiKeys`, `RevokeApiKey`, y el middleware `ingestKeyMiddleware` como fuente de verdad hasta completar este documento._
