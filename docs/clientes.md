# Clientes

**Leer cuando:** registros de cliente/empresa (CRM) y su relación con proyectos.

_Todavía no documentado en detalle — leer `ClientController.ts` y los casos de uso `CreateClient`, `GetClient`, `ListClients`, `UpdateClient`, `DeleteClient` como fuente de verdad hasta completar este documento._
